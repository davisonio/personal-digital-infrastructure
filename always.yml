---

- name: Run 'always' tasks on always_base
  hosts: okay_base
  roles:
    - ping
    - timezone
    - ntp
    - users
    - sudo
    - hostname
    - apt
    - openssh

- name: Run 'always' tasks on always_minetest
  hosts: okay_minetest
  roles:
    - minetest
