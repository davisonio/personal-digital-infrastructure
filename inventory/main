#
# Main hosts file
#
# Host naming scheme:
# <(optional) sub project>.<server type><incremental number>.<environment>.<domain>.<tld>
#
# Server types in current use:
# srv = server
# web = web server (e.g. nginx or apache)
#
# Environment types in current use:
# dev = development
# tst = testing
# stg = staging
# prd = production
#

[davisonio]
web1.prd.davison.io
minetest.srv1.prd.davison.io
